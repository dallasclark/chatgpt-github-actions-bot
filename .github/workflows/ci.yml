name: Deployment

on:
  push:
    branches:
      - develop

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-action:
    name: Test Action
    uses: ./.github/workflows/run-action.yml
    secrets: inherit